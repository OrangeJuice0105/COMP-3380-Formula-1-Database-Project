# SQLs

This is the directory for our refactored SQLs. The original data extracted from Kaggle had many redundant and invalid entries, hence these SQLs were created to repopulate the database with valid ones instead.